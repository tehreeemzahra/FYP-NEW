# Production API for App Store / Play Store builds (HTTPS required for iOS review)
# Copy to .env.production before: npm run build:app:store && npm run cap:sync
VITE_API_URL=https://cyberquest-api-jd5l.onrender.com
